#include <bits/stdc++.h>
using namespace std;

#define int long long
using ll = long long;
#define INF (int)1e18
#define f first
#define s second
#define all(x) (x).begin(), (x).end()

#define rall(x) (x).rbegin(), (x).rend()
using vll = vector<long long>;
using vi = vector<int>;
using vc = vector<char>;
using vs = vector<string>;
ll MOD = 1e9+7;
constexpr int inf = 1e18;
#define pb push_back
#define pf push_front
#define rep1(i,n) for(int i=1;i<=(n);i++)
#define rep(i,n) for (int i=0;i<(n);i++)
mt19937_64 RNG(chrono::steady_clock::now().time_since_epoch().count());

void solve(){
        //int n;cin >> n;
        string x;cin >> x;
        int cnt_a = 0;
        int n = x.length();
        rep(i,n){
                if (x[i]=='A')cnt_a++;
        }

        //check bb
        //
        bool has = false;
        for (int i=0;i<n-1;i++){
                if (x[i]=='B' && x[i+1]=='B')has = true;
        }
        // find the continuous blocks

        vi v;
        int len=0;
        for (int i=0;i<n;i++){
                if (x[i]=='A')len++;
                else{
                        v.pb(len);
                        len=0;
                }
        }
        if(len>0)v.pb(len);

        int mini = *min_element(all(v));
        if (x[0]=='B' || has || x[n-1]=='B'){
                cout << cnt_a;
        }
        else{
                cout << cnt_a - mini;
        }

        cout << endl;
}
int32_t main() {
    ios_base::sync_with_stdio(0);

    cin.tie(0);
    int t =1;


    cin >>t;

    while(t--){
    solve();
    }
    return 0;
}
