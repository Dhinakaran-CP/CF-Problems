#include <bits/stdc++.h>
using namespace std;

#define int long long
using ll = long long;
#define INF (int)1e18
#define f first
#define s second
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
using vll = vector<long long>;
using vi = vector<int>;
using vc = vector<char>;
using vs = vector<string>;
ll MOD = 1e9+7;
constexpr int inf = 1e18;
#define pb push_back
#define pf push_front
#define rep1(i,n) for(int i=1;i<=(n);i++)
#define rep(i,n) for (int i=0;i<(n);i++)
mt19937_64 RNG(chrono::steady_clock::now().time_since_epoch().count());

void solve(){

    ll n;
    cin >> n;
    
    vi rem(3,0);
    while(n>0){
        int d = n%10;
        n/=10;
        rem[d%3]++;
    }
    int x = rem[0]+rem[1]+rem[2];

    int sum = (rem[1]*1 + rem[2]*2)%3;


    if (sum == 0){
        cout << 0 ;
        return;
    }

    if (sum ==1){
        if (rem[1] >= 1 && x>1){
            cout << 1;
            return;
        }   
        else if (rem[2] >=2 && x>2){
            cout << 2;
            return;
        }

        cout << -1;
        return;
    }
    else if (sum == 2){
        
        if (rem[2]>=1 && x>1){
            cout << 1;return;
        }
        else if ( rem[1]>=2 && x>2){
            cout << 1 ;
            return;
        }

        cout << -1;
        return;
    }



}

int32_t main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int t = 1;
   // cin >> t;

    while(t--){
        solve();
    }
    return 0;
}
